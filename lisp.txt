(let x 45
  (tagbody
    (def foo (a)
      (tagbody
        (setq x (- x a))))
      
    (def bar (a)
      (setq x (+ x a)))))

(def main ()
  (tagbody 
    (foo 2)
    (bar 4)
    (foo 1)
    (bar 8)
    (foo 10)))