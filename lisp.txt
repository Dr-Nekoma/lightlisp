(def foo (lambda (a)
           a))

(def bar (lambda (x)
           (lambda (y)
             (+ x y))))           

(def lisp_main (lambda ()
                 (let b (bar 5)
                   (* (foo 2) (b 9)))))